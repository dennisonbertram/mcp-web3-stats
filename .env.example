# =================================================================
# MCP Web3 Stats Server - Environment Configuration
# =================================================================

# -----------------------------------------------------------------
# Required Configuration
# -----------------------------------------------------------------

# Dune Analytics API Key (Required)
# Get your key from: https://docs.dune.com/api-reference/overview/authentication
DUNE_API_KEY=your_dune_api_key_here

# -----------------------------------------------------------------
# Server Configuration (HTTP Transport)
# -----------------------------------------------------------------

# Bind host for HTTP server
# - Use 127.0.0.1 for local development (default)
# - Use 0.0.0.0 for Railway/cloud deployment
# MCP_BIND_HOST=127.0.0.1

# Server port (Railway sets this automatically)
# PORT=3000

# -----------------------------------------------------------------
# Authentication Configuration (Production)
# -----------------------------------------------------------------

# Enable authentication (recommended for public deployments)
# Set to 'true' when deploying to Railway or any public environment
# MCP_AUTH_ENABLED=false

# API Key Authentication
# Comma-separated list of valid API keys
# Generate keys with: node -e "console.log('mcp_' + require('crypto').randomBytes(32).toString('hex'))"
# Example: MCP_API_KEYS=mcp_abc123...,mcp_xyz789...
# MCP_API_KEYS=

# Basic Authentication (alternative to API keys)
# Comma-separated username:password pairs
# Example: MCP_BASIC_AUTH=admin:securepass123,user2:anotherpass456
# MCP_BASIC_AUTH=

# -----------------------------------------------------------------
# Example Configurations
# -----------------------------------------------------------------

# LOCAL DEVELOPMENT (stdio mode - default)
# No additional configuration needed
# Run: bun start

# LOCAL DEVELOPMENT (HTTP mode)
# MCP_AUTH_ENABLED=false
# Run: bun start -- --transport http --port 3000

# RAILWAY DEPLOYMENT
# DUNE_API_KEY=your_actual_key
# MCP_BIND_HOST=0.0.0.0
# MCP_AUTH_ENABLED=true
# MCP_API_KEYS=mcp_generated_key_1,mcp_generated_key_2
# PORT will be set automatically by Railway

# -----------------------------------------------------------------
# Notes
# -----------------------------------------------------------------

# 1. Never commit this file with actual secrets
# 2. Copy to .env and fill in your actual values
# 3. Add .env to .gitignore (already done)
# 4. Rotate API keys periodically
# 5. Use different keys for different environments
